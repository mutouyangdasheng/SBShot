# SBShot 项目规则文档

## 1. 项目定位

SBShot 是一个使用原生 C++ 开发的 Windows 轻量级截图工具，必须兼容 Windows 7 SP1、Windows 10 和 Windows 11。

项目优先级固定为：

1. 系统兼容性和稳定性。
2. 截图结果正确性。
3. 低内存、低 CPU 和快速响应。
4. 发布包及 EXE 体积。
5. 扩展功能和视觉效果。

任何新功能不得以明显增加常驻资源、启动时间、依赖数量或兼容风险为代价。

## 2. 技术基线

- 开发语言固定为 C++17，不引入需要更新系统运行时的语言扩展。
- UI 和系统交互固定使用 Win32 API，不使用 MFC、Qt、WinUI、UWP、Windows App SDK、Electron、CEF 或其他大型框架。
- 编译器优先使用 MSVC；编译工具链、Windows SDK 和链接参数必须锁定版本。
- 最低系统宏统一设置为 `_WIN32_WINNT=0x0601` 和 `WINVER=0x0601`。
- 发布程序使用 Unicode，统一调用 Win32 的 `W` 版本 API，不依赖系统 ANSI 代码页。
- 只动态依赖 Windows 7 自带的稳定系统组件，例如 `kernel32.dll`、`user32.dll`、`gdi32.dll`、`shell32.dll`、`ole32.dll` 和 Windows Imaging Component。
- 默认使用静态 MSVC 运行库 `/MT` 或 `/MTd`，正式发布不得要求用户额外安装 VC++ Redistributable。
- 不使用自定义 CRT 入口点等高风险体积优化；体积优化不得破坏全局对象初始化、异常处理、TLS 或系统兼容性。
- 工具链升级后，必须重新通过 Windows 7 SP1 干净虚拟机测试，未经验证不得更新正式构建基线。

## 3. 依赖规则

- 核心程序原则上保持零第三方运行时依赖，发布形态优先为单个 EXE。
- 可以使用仅参与构建、不会进入运行时的工具，例如 CMake、资源编译器和静态分析工具。
- 引入第三方 C/C++ 库前，必须证明系统 API 或少量自研代码无法合理解决问题。
- 每个运行时依赖都必须记录用途、许可证、二进制增量、内存增量、Win7 支持情况和移除方案。
- 禁止为了单个功能引入通用 GUI、网络、日志、JSON、图像或脚本运行时。
- 核心截图流程不得依赖网络、WebView、服务进程、驱动或管理员权限。

## 4. 架构规则

- 程序采用单进程、单实例、事件驱动架构。
- UI 主线程使用 Win32 消息循环，空闲时必须阻塞等待消息，禁止轮询和空转。
- 不常驻工作线程；只允许为明确的短时任务创建线程，任务结束后立即退出。
- 平台 API 必须集中在独立模块中，不能散落在界面和业务代码里。
- 截图、选区、标注、编码、剪贴板、快捷键、配置和资源管理必须保持清晰边界。
- 系统句柄和 COM 对象使用轻量 RAII 封装，所有权必须明确。
- 不为“以后可能使用”预建插件系统、服务层、通用事件总线或复杂抽象。

建议目录结构：

```text
SBShot/
  CMakeLists.txt
  PROJECT_RULES.md
  README.md
  assets/
  docs/
  src/
    app/
    capture/
    editor/
    platform/
    storage/
    ui/
  tests/
  scripts/
```

## 5. 兼容性规则

- 必须支持 Windows 7 SP1、Windows 10 和 Windows 11 的 x86、x64 桌面环境。
- x86 与 x64 分别构建原生 EXE，不使用一个进程混合架构。
- 不得静态导入 Windows 8/10/11 才存在的 API。
- 使用新系统 API 时，必须通过 `GetProcAddress` 运行时探测，并提供 Win7 可用的降级路径。
- 不使用未公开 API、注入、全局钩子、内核驱动或依赖系统实现细节的方案。
- 程序必须在普通用户权限下完成启动、截图、保存、剪贴板和快捷键操作。
- 兼容不同主题、经典主题、远程桌面和虚拟机场景，无法捕获的受保护内容应明确失败，不得崩溃。
- 应用清单必须声明正确的 Windows 兼容性和 DPI 行为，且不得默认请求管理员权限。

## 6. DPI 与多显示器规则

- 坐标统一以 Windows 虚拟屏幕坐标为基础，不能假设主屏左上角为 `(0, 0)`。
- 必须正确处理左侧或上方显示器产生的负坐标。
- 必须明确区分逻辑坐标、设备像素和截图位图像素。
- Windows 10/11 优先启用 Per-Monitor DPI Awareness；Windows 7 使用其支持的 DPI 模式并提供一致的坐标换算。
- DPI API 必须运行时探测，不得因缺少新 API 导致 Win7 启动失败。
- 至少验证 100%、125%、150%、175% 和 200% 缩放。
- 必须覆盖不同显示器使用不同 DPI、分辨率和排列方向的场景。
- 跨屏选区不得偏移、拉伸、丢失边缘像素或产生无效区域。

## 7. 截图实现规则

- 第一阶段使用 Win32 GDI 捕获作为兼容基线，优先保证 Win7 至 Win11 行为一致。
- 默认截图能力包括区域截图、全屏截图、活动窗口截图和跨显示器截图。
- 捕获流程必须正确处理虚拟屏幕尺寸、窗口边框、分层窗口和光标是否入图的配置。
- 覆盖层显示前应缓存屏幕底图，避免覆盖层被捕获进结果，也避免选区过程中反复截取整个桌面。
- 高级捕获 API 只能作为新系统上的可选加速路径，失败时必须自动回退到兼容路径。
- 对受 DRM、硬件覆盖或系统保护的窗口，不承诺绕过保护；失败不得影响普通截图。
- 默认输出 PNG；图像编码优先使用 Windows Imaging Component，不捆绑大型图像库。
- 截图位图应尽量复用，禁止在选区拖动或标注刷新时反复复制整张大图。
- 所有 `HDC`、`HBITMAP`、`HBRUSH`、`HPEN`、`HFONT`、`HRGN` 和 COM 对象必须按所有权及时释放。

## 8. 功能范围规则

第一阶段核心功能：

- 全局快捷键唤起。
- 区域、全屏、活动窗口和多显示器截图。
- 复制到剪贴板。
- 保存 PNG 文件。
- 取消截图。
- 单实例运行和托盘退出。

第二阶段基础标注：

- 矩形、箭头、画笔、文字。
- 马赛克或模糊二选一，优先实现资源消耗更低且效果稳定的方案。
- 撤销和重做，历史记录数量必须有上限。

以下功能默认不进入核心版本：

- OCR、云上传、账号体系、插件系统、脚本引擎。
- 视频录制、GIF 编码、复杂滤镜和 AI 功能。
- 无上限截图历史、常驻缩略图库和后台目录扫描。

长截图和贴图功能需单独评估体积、内存和兼容性后才能立项。

## 9. 快捷键与单实例规则

- 全局快捷键使用 `RegisterHotKey`，不得为此安装键盘钩子。
- 默认快捷键不得与常见 Windows 系统快捷键冲突，并允许用户修改。
- 注册失败时必须提示，不能静默失效。
- 程序退出时必须注销快捷键并释放托盘资源。
- 单实例优先使用命名互斥量和轻量 IPC；第二实例只负责通知已有实例后退出。
- IPC 必须设置超时，不能因已有实例无响应而永久阻塞。

## 10. 配置、日志与文件规则

- 配置保持简单，优先使用体积小、可容错的 INI 或紧凑二进制格式；不为配置引入 JSON 库。
- 默认配置写入 `%APPDATA%\SBShot`，便携模式可由明确标志启用并写入程序目录。
- 默认截图目录使用用户图片目录，文件名包含毫秒级时间戳并避免覆盖。
- 保存过程应先写临时文件，再以可恢复方式完成替换；失败时截图仍可复制到剪贴板或重新保存。
- 默认关闭详细日志，仅记录启动失败、API 失败和保存失败等必要信息。
- 日志必须限制单文件大小和保留数量，不记录截图内容、剪贴板内容或敏感文本。
- 临时位图和临时文件使用后立即清理，异常退出后的残留应在下次启动时限量清理。

## 11. 性能与资源预算

以下指标以 Release x64、单显示器 1920x1080、程序空闲 30 秒后的稳定状态为基准。工作集受系统版本影响，仅作为回归门槛，主要比较 Private Bytes 和句柄数量。

- 冷启动到托盘可用目标不超过 200 ms，Win7 机械硬盘环境允许单独记录较宽基线。
- 空闲 CPU 平均占用目标为 0%，不得存在固定频率轮询定时器；短暂系统消息唤醒除外。
- 空闲 Private Bytes 目标不超过 10 MB，超过 15 MB 视为性能回归，必须说明原因。
- 空闲工作集目标不超过 20 MB，不以主动频繁裁剪工作集伪造指标。
- 空闲线程数目标不超过 2 个，GDI 对象和 USER 对象应保持稳定。
- 热键触发到选区覆盖层可交互的目标不超过 100 ms，不包含系统首次加载带来的冷缓存差异。
- 一次截图完成并关闭编辑器后，Private Bytes、GDI 对象、USER 对象和句柄数必须回落到稳定基线，连续 100 次操作不得持续增长。
- 大图内存按“原始像素数据 + 必要工作缓冲”核算；核心流程同时存在的全尺寸 32 位位图原则上不超过 2 份。
- 标注重绘只刷新脏区域，拖动期间不得重复编码 PNG、写磁盘或复制完整位图。
- 配置保存、日志写入和文件清理不得放在高频鼠标移动路径中。

## 12. EXE 与发布包体积规则

- Release 构建启用尺寸优化和链接裁剪，建议使用 `/O1`、`/Gy`、`/Gw`、`/GL`、`/LTCG`、`/OPT:REF` 和 `/OPT:ICF`，最终参数以实测正确性为准。
- 发布 EXE 不包含 PDB、测试代码、调试字符串、未使用资源和开发期诊断界面；PDB 作为独立调试符号归档。
- 不嵌入高分辨率无损宣传图片、字体、音效、多套重复图标或大型语言资源。
- 禁止使用 UPX 等可执行文件压缩器，避免杀毒误报、启动额外开销和内存映射问题。
- 默认 `/MT` 是兼容性决策。不得为了让 EXE 数字更小改为 `/MD`，却把 VC 运行库依赖转嫁给用户。
- 应同时记录 EXE 文件大小和完整发布包大小，优化时以可独立运行的完整发布物为准。
- 首个核心版本的 x64 单 EXE 体积目标不超过 1 MB；超过目标必须列出资源和代码贡献，禁止盲目压缩。
- 每次发布记录 x86/x64 EXE 大小，单次普通功能修改增长超过 10% 时必须审查。

## 13. C++ 代码规范

- 禁止在业务代码中使用裸 `new`/`delete`；优先使用栈对象、明确所有权和轻量智能指针。
- 禁止无边界的容器、缓存、撤销栈、日志队列和线程队列。
- 热路径避免频繁堆分配、字符串格式化、隐式位图复制和锁竞争。
- 不使用 RTTI；异常是否启用由模块决定，核心 Win32 边界优先使用显式错误码或结果类型。
- 不跨 DLL 边界传递 STL 类型；核心版本原则上不拆分自有 DLL。
- 所有 Win32 API 返回值必须检查，错误必须保留足够上下文用于诊断。
- 尺寸和坐标计算使用明确宽度的整数类型，并检查乘法溢出及负坐标。
- 字符串统一使用 UTF-16 `wchar_t` 与 Win32 API 交互，文件内容编码必须在格式中明确。
- 注释解释约束和原因，不复述代码；禁止提交废弃代码、临时调试逻辑和本地绝对路径。
- 编译警告等级至少 `/W4`，项目代码警告视为错误；第三方或系统头警告必须隔离。

## 14. 稳定性与安全规则

- 启动、快捷键、截图、图像编码、剪贴板和文件保存必须处理全部失败路径。
- 任何尺寸来自系统、窗口或文件的数据在分配内存前必须检查范围和溢出。
- 剪贴板打开失败时应进行有限次数、带短间隔的重试，禁止无限等待。
- 崩溃或高级功能失败时，不得破坏用户原有剪贴板内容或遗留全屏置顶窗口。
- 截图内容默认只在本地处理，未经用户明确操作不得联网或上传。
- 正式核心版本默认不包含网络代码；未来增加网络功能必须独立模块化并可在构建时完全关闭。
- 不记录截图像素、OCR 文本、剪贴板数据或用户输入的标注文字。
- 发布前必须进行静态分析、基础安全扫描和常见杀毒软件误报检查。

## 15. 测试规则

每次影响核心流程的修改至少验证：

- Windows 7 SP1 x86/x64 启动、热键、区域截图、PNG 保存和剪贴板。
- Windows 10 x64 与 Windows 11 x64 的同等流程。
- 主屏位于不同位置、含负坐标的多显示器布局。
- 100%、125%、150%、175% 和 200% DPI。
- 不同 DPI 显示器之间的跨屏选区。
- 活动窗口、分层窗口、经典主题、远程桌面和全屏应用。
- 快速连续截图、取消、保存失败、快捷键冲突和第二实例启动。
- 4K 单屏和多屏大图的内存峰值。
- 连续截图 100 次后的 Private Bytes、线程、句柄、GDI 对象和 USER 对象变化。

无法自动化的 GUI 和跨系统测试必须维护手工测试清单；能从坐标、编码和资源所有权中拆出的逻辑应做单元测试。

## 16. 构建规则

- 构建必须可由一条脚本命令完成，并同时支持 Debug、Release、x86 和 x64。
- Release 使用 Windows GUI 子系统，不显示控制台窗口。
- 构建必须生成内嵌版本信息、应用清单和图标。
- 正式产物必须可在无开发工具、无额外运行库的干净 Win7 SP1 虚拟机中启动。
- 构建脚本必须输出编译器版本、SDK 版本、架构、Git 提交号和产物哈希。
- 不在源码仓库提交编译产物、PDB、用户配置、截图和本地缓存。
- x86 与 x64 分开发布并明确命名；不优先制作安装器，先提供免安装单 EXE。

## 17. 发布与版本规则

- 版本号使用 `主版本.次版本.修订号`。
- 发布前必须在 Windows 7 SP1、Windows 10 和 Windows 11 的干净环境完成冒烟测试。
- 发布说明必须记录系统支持范围、架构、文件大小、SHA-256、已知限制和升级注意事项。
- 兼容性或资源预算发生变化时，必须在发布说明中明确列出。
- 不承诺未实际验证的 Windows 小版本或特殊精简系统。

## 18. 文档与决策记录

- `README.md` 必须说明支持系统、功能、快捷键、构建方法、运行方式和已知限制。
- `docs/compatibility.md` 记录 Win7 API 基线、DPI 策略、捕获降级路径和测试矩阵。
- `docs/performance.md` 记录 EXE 大小、启动时间、空闲资源和截图峰值的版本基线。
- 新依赖、新线程、新常驻缓存、新系统 API 或超过 50 KB 的资源必须留下简短决策记录。
- 构建和发布步骤必须可复现，不能依赖开发者机器上的未记录状态。

## 19. 开发顺序

1. 建立 Win32 C++17 项目骨架和 x86/x64 Release 构建。
2. 验证最小程序在 Win7 SP1、Win10、Win11 启动，并建立体积和资源基线。
3. 实现 GDI 屏幕捕获、虚拟屏幕坐标和多显示器支持。
4. 实现覆盖层、区域选择、取消和快捷键。
5. 实现 WIC PNG 编码、文件保存和剪贴板复制。
6. 完成 DPI 适配和跨 DPI 显示器验证。
7. 加入托盘、单实例和最小配置。
8. 完成资源泄漏、连续截图和性能回归测试。
9. 核心版本稳定后再实现基础标注。
10. 任何扩展功能立项前重新评估兼容性、EXE 增量和运行资源。
